{% extends "layout.j2" %}
{% block header %}
    <div class="panel panel-default">
        <div class="panel-body">
            {# We'll do an if logged in, then do this navbar, else
                else do other #}
            {%  include 'nav_bar_index.j2' %}
            {%  include 'nav_bar_tech.j2' %}
            {%  include 'search.j2' %}
            {%  include 'nav_bar_dev.j2' %}
        </div>
    </div>
{% endblock %}

{% block body %}


    <div class="row">

        <div class="col-lg-12">

            <div class="panel panel-default panel-table">
              <div class="panel-heading text-center">Teachers Table</div>

              <div class="panel-body">
                <table class="table table-striped table-bordered table-list">
                  <thead>
                    <tr>
                        <th></th>  {# empty header for the radio button column#}
                        <th>ID</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Phone</th>
                        <th>Email</th>
                        <th>Organization</th>
                    </tr>
                  </thead>

                  <tbody>

                        {# michaelwashere #}
                        {# put a loop here filling each field from database#}
                        {#make sure you only grab teachers from the database#}

                          <tr>
                            <td>
                              <div class="radio">
                                    {# I would find a way to tie the name of the radio button to a location in the table
                                        and then tie that location to the delete button below#}
                                  <label><input type="radio" name="optradio"></label>
                              </div>

                            </td>
                            <td>ID Here</td>
                            <td>First Name Here</td>
                            <td>Last Name Here</td>
                            <td>Phone Here</td>
                            <td>Email Here</td>
                            <td>Organization Here</td>
                          </tr>
                        {# end loop here#}

                  </tbody>

                </table>

              </div>

              <div class="panel-footer">
                <div class="row">
                    <div class="col-12">

                        {# michaelwashere #}
                        {# tie this button to the proper radio button and redirect to 'tech_change_teacher_password' page while passing selected user info#}

                        <button type="button" class="btn btn-default btn-danger">Change Selected Teacher's Password</button>

                    </div>

                </div>
              </div>
            </div>
        </div>
    </div>

{% endblock %}

{% block footer %}
    {# <footer><p><a href="mailto:trey.slaughter@gmail.com">Contact Us</a></p></footer> #}
{% endblock %}