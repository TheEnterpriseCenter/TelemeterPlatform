<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ url_for("index") }}">
            MTC4K
            {% if session.logged_in %}
                {% if current_user.role == 'teacher' %}
                    : Teacher
                {% elif current_user.role == 'technician' %}
                    : Technician
                {% elif current_user.role == 'admin' %}
                    : Administrator
                {% endif %}
            {% endif %}
            </a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                {% if session.logged_in %}
                    {% if current_user.role == "admin" %}
                        <ul class="nav navbar-nav">
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Create New<span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                    <li><a href="{{ url_for('admin_create_new_user') }}">User</a></li>
                                    <li><a href="{{ url_for('admin_create_new_asset') }}">Asset</a></li>
                                    <li><a href="{{ url_for('admin_create_new_upload_acct') }}">Upload Account</a></li>
                                </ul>
                            </li>
                        </ul>
                        <ul class="nav navbar-nav">
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Modify/Delete<span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                    <li><a href="{{ url_for('admin_delete_user') }}">Users</a></li>
                                    <li><a href="{{ url_for('admin_delete_asset') }}">Assets</a></li>
                                    <li><a href="{{ url_for('admin_delete_upload_acct') }}">Upload Accounts</a></li>
                                    <li><a href="{{ url_for('admin_delete_experiment') }}">Experiments</a></li>
                                    <li><a href="{{ url_for('admin_delete_tag') }}">Experiment Tags</a></li>
                                </ul>
                            </li>
                        </ul>
                    {% endif %}
                    {% if current_user.role == "technician" %}
                        <ul class="nav navbar-nav">
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Create New<span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                    <li><a href="{{ url_for('tech_create_new_teacher') }}">Teacher</a></li>
                                </ul>
                            </li>
                        </ul>
                        <ul class="nav navbar-nav">
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Modify/Delete<span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                    <li><a href="{{ url_for('tech_delete_teacher') }}">Teachers</a></li>
                                </ul>
                            </li>
                        </ul>
                    {% endif %}
                {% endif %}
                {% if session.logged_in %}
                    <li><a href="{{ url_for('profile') }}">Profile</a></li>
                {% endif %}
                {% if session.logged_in %}
                    <li><a href="{{ url_for('logout') }}">Logout</a></li>
                {% else %}
                    <li><a href="{{ url_for('login') }}">Login</a></li>
                {% endif %}
                <li><a href="{{ url_for('contact_us') }}">Contact Us</a></li>
            </ul>
        </div>
    </div>
</nav>